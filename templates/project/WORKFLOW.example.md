# <YOUR_PROJECT> Workflow

## 本项目默认顺序

1. 识别双目录
2. 判断项目类型和默认稳定方案
3. 如果是 Web，默认采用 `最小 CI + 预览环境 + 手动正式发布`
4. 执行 `new -> old` 基线快照
5. 回看基线文档
6. 新阶段先走文档链路
7. 在 PRD 和技术文档里补齐未来扩展对齐
8. 确认目录预留方案
9. 再进入拆解、派单和实现
10. 每轮结束验证并汇报

## 里程碑回看

以下节点前重新读取基线文档：

- 新的大功能开始前
- 架构边界变化前
- 接入真实外部服务前
- QA 和发布前
